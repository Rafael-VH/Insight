class HeroStat {
  final String label;
  final String value;
  final double normalizedValue;

  const HeroStat({
    required this.label,
    required this.value,
    required this.normalizedValue,
  });
}
